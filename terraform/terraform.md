# Terraform Notes

This directory contains Terraform code to provision:

- EKS cluster
- VPC (public + private subnets)
- IAM & IRSA integration
- Node groups
- OIDC provider

Use:

```
terraform init
terraform plan
terraform apply
```